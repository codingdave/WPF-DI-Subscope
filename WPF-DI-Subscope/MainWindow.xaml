<Window x:Class="WPF_DI_Subscope.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_DI_Subscope"
        mc:Ignorable="d"
        d:DataContext="local:MainWindowViewModel"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Content="Project" FontSize="24" HorizontalAlignment="Center"/>
        <UserControl Grid.Row="1" Content="{Binding Project.Guid}"/>
        <Button Grid.Row="2" Margin="5,10,5,10" HorizontalAlignment="Right" 
                Command="{Binding NewProjectCommand}" Content="New Project"/>
    </Grid>
</Window>
